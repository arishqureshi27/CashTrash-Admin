import 'package:flutter/material.dart';

class PickUpRequest extends StatelessWidget {
  const PickUpRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Pick Up Request'),
    );
  }
}
